# Cove 实施决策记录

> 本文件记录对开发基线（`docs/COVE-DEVELOPMENT-GUIDE.md`）的实施级决策与后续变更。
> 架构级决策 ADR-001 ~ ADR-010 以指南第 23 节为准，此处不重复维护。
> 修改已确认决策时，必须新增条目写明原因、影响范围和迁移方案，不直接覆盖历史判断。

## 记录格式

| 编号 | 日期 | 决策 | 原因 | 影响范围 |
| --- | --- | --- | --- | --- |

## 实施决策

| 编号 | 日期 | 决策 | 原因 | 影响范围 |
| --- | --- | --- | --- | --- |
| IMPL-001 | 2026-09-16 | 工具链版本：Astro 5 + Vue 3.5 + Tailwind CSS 4（`@tailwindcss/vite`，CSS-first 配置）+ TypeScript strict | 以当前主版本官方 API 为准（指南 11.1）；Tailwind v4 无 `tailwind.config.js`，设计令牌经 `@theme inline` 映射 CSS variables | 全站样式与构建链 |
| IMPL-002 | 2026-09-16 | 路径别名 `@/*` → `src/*`（tsconfig paths，Astro 原生解析） | 指南第 6 节推荐结构要求稳定别名 | 全部源码导入 |
| IMPL-003 | 2026-09-16 | shadcn-vue 仅完成初始化（`components.json` + `src/lib/utils.ts` 的 `cn`），本阶段不添加任何 UI 组件 | 指南 Phase 1 要求“只添加当前阶段真正使用的组件”；Button/IconButton/Tag/Divider 均为 Astro 原语，无需 Vue runtime | 后续 Phase 3/4 按需添加 Sheet、Command、Dialog 等 |
| IMPL-004 | 2026-09-16 | 主题方案：`localStorage` 键 `cove:theme`（`light` / `dark` / `system`），`<html>` 同时携带 `data-theme`（三态）与 `.dark` class（二态）；head 内联脚本在绘制前恢复，防闪烁 | 满足指南 9.1（浅色/深色/跟随系统）与 10（`ThemeScript` 原生内联脚本）；`data-theme` 供无 JS 场景的图标显示与 CSS 选择 | `ThemeScript`、`ThemeToggle`、Tailwind `dark` 变体 |
| IMPL-005 | 2026-09-16 | `astro.config.mjs` 的 `site` 与 `src/data/site.ts` 中作者名、简介、社交链接、正式域名均使用明确标记的占位值 | Phase 0 需作者确认真实信息；占位集中在 `src/data/site.ts`，避免散落 | Phase 5 SEO 与 Phase 7 部署前必须替换 |
| IMPL-006 | 2026-09-16 | `build` 脚本暂为 `astro check && astro build`，不含 `pagefind` | Pagefind 属 Phase 4 交付；提前引入会让无索引内容阶段产生空索引误导验收 | Phase 4 将 `build:search` 并入 `build` 并锁定 wrapper 版本 |
| IMPL-007 | 2026-09-16 | 组件展示页置于 `/dev/`，始终输出 `noindex` | Phase 1 交付物需要可检查的令牌与原语页面，但不得进入搜索索引 | `src/pages/dev/index.astro` |
| IMPL-008 | 2026-09-16 | 本阶段页头搜索按钮渲染为指向 `/search/` 的原生链接（带快捷键提示） | 搜索对话框属 Phase 4；先用无 JS 可用的链接占位，保持渐进增强 | `SiteHeader`，Phase 4 替换为 Vue SearchDialog |
| IMPL-009 | 2026-09-16 | 移动导航暂以可换行的原生链接列表实现，不引入 Vue Sheet | `MobileNav`（Vue + Sheet）属 Phase 3 交付，届时与真实页面一起验收焦点管理与滚动锁定 | `SiteHeader` |
| IMPL-010 | 2026-09-16 | 主题按钮图标渲染改为“系统图标为 HTML 默认 + 浅/深图标携带原生 `hidden` 属性”，状态规则移至 `global.css` | 修复视觉验收反馈：原实现三个图标均渲染且仅靠外部 CSS 隐藏，CSS 应用前的一瞬间会同时闪现多个图标（FOUC）；`hidden` 属性由 UA 样式表在任何 CSS 加载前生效，保证任意时刻最多显示一个图标。注意不得给 `.theme-icon` 写统一 display 规则，否则会覆盖 `hidden` 的 UA 默认值 | `ThemeToggle`、`global.css` |
| IMPL-011 | 2026-09-16 | 主题简化为两态：删除“跟随系统”模式，仅保留浅色/深色，点击在两态间切换并持久化 `cove:theme`；首次访问（无存值）由引导脚本按 `prefers-color-scheme` 作一次性初始值；图标仅剩 Sun/Moon，状态规则移出 `@layer`（未分层）并彻底弃用 `hidden` 属性；BaseLayout 引导脚本在首绘前注入一份镜像规则兜底 | 用户决策：简化交互（覆盖指南 9.1 的三态要求，IMPL-004/010 中三态与 `hidden` 兜底描述作废）。同时修复 IMPL-010 方案缺陷：Tailwind preflight 的 `[hidden]{display:none!important}` 为重要声明，非 important 的 `display:flex` 无法覆盖，导致图标全部消失；未分层规则亦可抵御 HMR 残留的分层样式干扰 | `ThemeToggle`、`BaseLayout`、`global.css`、`MonitorIcon`（删除） |
| IMPL-012 | 2026-09-16 | 默认社交分享图：`scripts/og-default.svg` 为源，`node scripts/generate-og.mjs` 用项目内 sharp（Astro 既有依赖，无新增）渲染 1200x630 PNG 提交为 `public/og-default.png`；SVG 内只渲染拉丁文本，中文 slogan 由 `og:image:alt` 承担 | 社交平台普遍不支持 SVG og:image；librsvg 环境字体差异会导致中文缺字，PNG 产物入库保证构建可复现且零构建期开销 | `scripts/`、`public/og-default.png`、`src/lib/seo.ts` |
| IMPL-013 | 2026-09-16 | 文章分享图规则：封面为位图（png/jpeg/webp）时用封面并输出真实宽高，SVG 封面回退默认图 | 主流平台不索引 SVG og:image，样本封面均为装饰性 SVG，回退保证分享卡片始终有有效图 | `ArticleLayout`、`src/lib/seo.ts` |
| IMPL-014 | 2026-09-16 | `robots.txt` 从静态 `public/` 迁移为 `src/pages/robots.txt.ts` 端点，sitemap 地址由 `siteConfig.url` 解析；不 `Disallow: /dev/` | 域名单一来源（IMPL-005 占位值替换时无需改两处）；Disallow 会阻止爬虫读取 /dev/ 的 noindex 标记，反而不利于去索引 | `src/pages/robots.txt.ts`（删除 `public/robots.txt`） |
| IMPL-015 | 2026-09-16 | JSON-LD：Person 全站由 BaseLayout 统一输出；BlogPosting + BreadcrumbList 仅文章页经 `jsonLd` prop 注入；数据全部来自内容 schema 与 siteConfig（指南 13.2），构建器集中在 `src/lib/seo.ts` | 避免页面手写重复信息；`siteConfig.social` 改为 `SocialLink[]` 显式标注（原 `satisfies + as const` 空数组被收窄为 `never` 元组导致类型错误） | `seo.ts`、`BaseLayout`、`ArticleLayout`、`site.ts` |
| IMPL-016 | 2026-09-16 | Web App Manifest 以静态 `public/site.webmanifest` 提供（name/theme_color 取自令牌色 `#3f789f`，icon 复用 favicon.svg），BaseLayout 统一 `<link rel="manifest">` | 指南 19 Phase 5 仅要求 manifest 存在；静态文件即可，不值得为此加端点；V1 不做完整 PWA（无 service worker），Phase 8 观察期再评估 | `public/site.webmanifest`、`BaseLayout` |
| IMPL-017 | 2026-09-16 | 全局加载体验（用户需求，基线未覆盖）：① `astro:transitions` ClientRouter 接管站内导航，旧页保留至新页就绪，消除整页白屏；② `AppSplash` 首次进入品牌加载页（Logo + 波浪动画 + tagline，sessionStorage `cove:splash-shown` 会话去重，解析期内联脚本先于首绘决定显示，`astro:page-load` 或 2500ms 兜底淡出，无 JS 永不出现）；③ `RouteProgress` 导航顶部 2px 进度条（120ms 阈值防闪，`astro:before-preparation` → `after-swap` 补满淡出，与阅读进度同视觉语言） | 用户决策：两个场景（首访 + 导航）都要、形态结合。代价：全站引入约 10KB 路由 JS，超出指南 14「首页初始 JS 只包含主题与必要导航逻辑」的字面预算，记录为对基线的扩展；波浪/进度动画属功能性加载指示，作为指南 8.6「禁止循环动效」的受限例外，reduced-motion 下由全局规则退化为静态。连带改造：打包脚本在 ClientRouter 下仅执行一次，`ThemeToggle` 改 document 事件委托，`ArticleTOC`/`ArticleLayout` 增强改为 `astro:page-load` 初始化（监听器跨换页存活 + AbortController 清理）；主题镜像 style 加 id 防换页累积；放弃 `data-astro-rerun` 方案（任意属性会使脚本退化为 inline、禁用 TS） | `BaseLayout`、`AppSplash`、`RouteProgress`、`ThemeToggle`、`ArticleTOC`、`ArticleLayout`、`global.css` |
| IMPL-018 | 2026-09-16 | 修复 IMPL-017 验收发现的三个回归：① `AppSplash` 退出增加最小展示时长（600ms，自解析脚本执行起算）+ 双 `requestAnimationFrame`，确保至少一帧真实绘制后再淡出；② `BaseLayout` 主题引导脚本重构为 `resolveMode`/`criticalRules`/`applyTheme` 三函数，并在 `astro:after-swap` 重放（监听器挂 document 跨换页存活 + `window.__coveThemeBound` 防重复注册），换页后同步恢复 `dark` class / `data-theme` / `colorScheme` 并移除 `no-js`；③ 首绘前镜像规则从主题图标扩展为「图标 + 搜索回退可见性 + 加载页遮罩」（遮罩背景色按当前主题以字面量内联，`#111a20` / `#f8fbfc`），style id 更名 `cove-critical-rules` | 根因：ClientRouter 换页把 `<html>` 属性整体覆盖回服务端渲染态（`class="no-js"`、无 `dark`/`data-theme`），且内联主题脚本只在首访执行——导致深色被重置为浅色（bug 2）、`no-js` 残留使 `.search-fallback` 与 `.search-trigger`（Ctrl K 提示）同屏（bug 3），dev 模式 Vite 异步注入 CSS 放大闪现；加载页不可见（bug 1）是本地静态页 `astro:page-load` 先于首绘触发，原实现立即淡出 | `BaseLayout`、`AppSplash` |
| IMPL-019 | 2026-09-16 | 双搜索框真正根因修复：搜索回退链接从「始终渲染 + CSS 按类切换」改为 `<noscript>` 内渲染；删除 `.search-fallback` 的全部显示/隐藏 CSS（含 IMPL-018 加入镜像规则的两条），保留 `html.no-js` 下隐藏岛屿触发器的规则 | IMPL-018 对 bug 3 的诊断不完整：换页重放只解决 `no-js` 残留，真正原因是特异性冲突——组件 scoped 规则 `.search-entry[data-astro-cid]`（0,2,0）声明 `display:inline-flex`，恒压过全局 `.search-fallback{display:none}`（0,1,0），回退链接在任何有 JS 环境下从未被隐藏过。`<noscript>` 语义上就是「仅无 JS 时存在」：有 JS 不进渲染树、无 JS 由浏览器渲染，彻底退出样式竞争，也不再需要换页重放/镜像覆盖 | `SiteHeader`、`BaseLayout` |
| IMPL-020 | 2026-09-16 | 文章页布局重构（指南 8.7 / 9.4 落地修正）：`ArticleTOC` 从正文列内部移至 `.article-columns` 直接子级——其片段展开的 `.toc-aside` 经 `grid-area:toc` 进入右侧粘性栏，`.toc-mobile` 留在文档流（<1200px 正文前折叠）；页头/正文/上下篇统一在 45rem 内容列（与 `.prose` 行宽 720px 对齐，左缘一致），整体 `max-width:63rem` 居中。页眉呼吸感微调：高度 64→68px（指南区间内），导航项间距 0.25→0.75rem、内边距 0.75→1rem，动作区间距 0.25→0.5rem | 用户验收反馈：目录随滚动下移与正文重合、正文整页偏左。根因是 `ArticleTOC` 被放在 `.article-main` 内部正文上方——`.toc-aside` 的 `sticky top:96px` 在该位置表现为贴顶悬浮、正文从其下方穿过；网格声明的右侧 240px 列一直为空导致正文左移且与居中页头错位。重构后目录栏正是用户推断的「页面偏右」位置 | `ArticleLayout`、`SiteHeader` |
| IMPL-021 | 2026-09-17 | 归档页改为时间轴（用户需求）：左侧 2px 竖轨（`::before`，left:5px），年份节点为 12px 品牌蓝圆环（`--background` 填充 + 3px `--cove-blue` 边框），条目节点 6px 实心圆在 2rem 缩进列表内（`left:-29px`），三者圆心对齐 x=6px；条目为整行链接（MM-DD 等宽字体 + 标题 + 主分类右对齐），悬停仅颜色变化（节点点亮、日期变品牌蓝、标题变强调蓝加下划线）。卡片动效（指南 8.6 白名单内）：`ArticleCard`/`ProjectCard` 悬停 `translateY(-2px)` 上浮 + 品牌色描边（120–180ms）；`ArticleListItem`/`NoteListItem` 悬停品牌色 6% 淡底（负外边距 + 等量内边距让底色出血且不改排版位置）；`HomeSection`「查看全部」箭头悬停 2px 右推 | 归档路由表原定义「按年份和月份归档」，时间轴形式不改变信息结构（仍按年分组倒序），仅视觉重构；动效严格限定颜色/透明度/≤2px 位移与 120–220ms 时长，无循环无视差，reduced-motion 下由全局规则退化为瞬时 | `src/pages/archive/index.astro`、`ArticleCard`、`ProjectCard`、`ArticleListItem`、`NoteListItem`、`HomeSection` |
| IMPL-022 | 2026-09-17 | 时间轴与页尾视觉精修（用户需求）：① 归档时间轴补月份层——年内按月分块（`chunkByMonth`，相邻同月聚合），月份小标带轨上 11px 短刻度，年份篇数从括号文本改为 pill 徽标，竖轨两端 36px 渐隐，条目悬停增加品牌色 6% 淡底（与列表项同一视觉语言，负外边距出血，节点偏移相应 -29px→-17px），条目间边线移除；② 页脚重构——顶部双波浪饰线（呼应 Logo/加载页，`--cove-blue` + `--shell-pink`，`vector-effect:non-scaling-stroke` 保证描边粗细恒定），新增「分类」列（5 个分类直达页，补齐指南 7.1 要求的分类入口），版权行独立为 `footer-bottom` 底栏（细线分隔），移除页脚原 border-top 让波浪承担接缝 | 指南 7.2 路由表本就写明「按年份和月份归档」，IMPL-021 只做了年层，本次补齐；标签暂无索引页（`/tags/` 只有 `[tag].astro`），页脚「发现与订阅」暂不列标签，留待后续阶段；波浪为静态装饰非循环动效，不违反指南 8.6 | `src/pages/archive/index.astro`、`SiteFooter` |
| IMPL-023 | 2026-09-17 | 系列功能落地（指南 11.1 预留 `series` 字段的实现，用户需求）：同名 frontmatter 值聚合为系列，阅读顺序 = 发布顺序（`publishedAt` 升序，不引入 seriesOrder）；`lib/content.ts` 新增 `getAllSeries`/`getSeriesGroup`（系列间按最近更新倒序，slug 碰撞构建期抛错，与标签同策略），`urls.ts` 新增 `seriesUrl`；新增 `/series/` 索引页与 `/series/[series]/` 详情页（篇目带 01/02 序号，首篇序号品牌色）；`SeriesCard` 文内系列卡（正文上方：系列名链接 + 第 N/M 篇 + 系列内上一篇/下一篇，单篇系列不渲染前后链接）。测试内容：`astro-from-scratch-{1,2,3}.md`（三篇连续系列）+ `cove-retro-solo.md`（单篇边界），验收后可删 | 路由表新增 `/series/` 两条路由，记录为对基线的扩展（指南只预留字段未定义路由）；系列名即 slug 经 `slugifyTag`（保留中文），与标签共用规范化策略；测试文章两次构建失败教训：① 标签写法须与既有内容一致（`astro` 与 `Astro` 碰撞被拦，规范为品牌词首字母大写）；② posts 路由是单段 `[id]`，内容文件必须平铺（子目录使 id 带斜杠导致 `Missing parameter: id`），故测试文件平铺在 posts 根目录而非子目录 | `src/lib/content.ts`、`src/lib/urls.ts`、`src/pages/series/`、`SeriesCard`、`ArticleLayout`、`src/content/posts/astro-from-scratch-*.md`、`cove-retro-solo.md` |
| IMPL-024 | 2026-09-17 | `SeriesCard` 改为折叠式同系列文章列表（用户决策）：原生 `<details>/<summary>` 默认收起，展开后列出系列全部篇目——当前篇淡底 + 品牌色序号 + 「本篇」徽标（`aria-current="page"`，非链接），其余篇目可点击（负外边距出血悬停淡底）；摘要行 = 系列小标 + 名称 + 第 N/M 篇 + 旋转箭头（展开 180°，150ms）；展开时摘要与列表以虚线分隔。单篇系列不渲染折叠，仅静态摘要行。替换原「上一篇/下一篇」链接（完整列表是其功能超集，全站级上下篇仍在文末） | 用户明确：默认收起；不追求具象"书/章节"装饰（点线目录、日期列等方案被否），按站点既有设计语言实现。`<details>` 与移动端折叠目录同模式：零 JS、键盘原生支持、ClientRouter 换页兼容；箭头旋转遵循折叠目录先例属白名单内动效，reduced-motion 全局退化 | `SeriesCard` |
| IMPL-025 | 2026-09-17 | 首页 hero 新增品牌配图（用户需求）：用户选定插画（灯塔+光束+海浪+帆船）原图归档至 `src/assets/brand/cove-support-illustration.jpg`，sharp 方形裁切（extract 455,55,495,495）导出 `public/home-brand.webp`（495×495，q88，12KB）；hero 改双列网格（≥768px 文案左、图右，`clamp(200px,24vw,296px)`，圆角 24px + border），<768px 隐藏（移动端无「右侧」语义，保首屏紧凑）；深色模式 `opacity:.92` 收敛插画自带浅色底；`aria-hidden` 纯装饰 | 用户指定「裁剪后放首页顶部右侧」；原图 280KB 不留在 `public/`（会被原样发布且无页面引用）；24px 大圆角使其读作「应用图标卡」而非突兀浅色方块；隐藏而非缩小堆叠，避免移动端 CTA 下沉 | `src/pages/index.astro`、`public/home-brand.webp`、`src/assets/brand/` |
| IMPL-026 | 2026-09-17 | Logo/favicon 重设计（用户反馈旧图标不好看）：从用户选定插画提取「灯塔 + 光束 + 海浪」母题几何化——品牌蓝渐变圆角底（#6799bd→#3f789f）+ 白色灯塔剪影/海浪 + 贝壳粉塔顶/灯点 + 50% 透明光束；`favicon.svg`（64 viewBox，含灯点）与 `Logo.astro`（24 viewBox，省略灯点保小尺寸剪影清晰）同构；`Logo` 为页头/页脚/加载页共用组件，一处更新全站生效 | favicon 需在 16–32px 可读：白剪影于品牌蓝底的对比远优于旧「浅色线稿于浅底」方案；光束方向、粉/蓝配色与插画一致，品牌语言统一；og-default.svg 不含图形标志无需重生成；`site.webmanifest` 引用路径不变 | `public/favicon.svg`、`Logo.astro`、`src/assets/brand/README.md` |
| IMPL-027 | 2026-09-17 | 全站响应式完善（用户需求，探索代理审查后逐项修复）：① prose 长内容断行根因修复——`.prose` 加 `overflow-wrap:break-word`，`.prose a` 与行内 `code` 加 `anywhere`（长 URL/命令撑破 720px 行宽），补 `video/iframe max-width:100%`；② 转载说明 URL `anywhere` 断行 + 封面 `sizes` 740→720px（对齐实际渲染宽）；③ 归档条目/系列卡窄屏（<640px）取消 `margin-left:auto` 右推，长标题换行后分类/进度/徽标紧跟内容流；④ `ProjectCard` 封面固定 220px 改 `clamp(150px,26%,220px)`（640–800px 区间不挤占文字列）；⑤ `HomeSection` 区块头加 `flex-wrap`；⑥ `featured-grid` 与 `PrevNextNav` 纯 `fr` 列改 `minmax(0,…)`；⑦ hero 高度固定 380px 改 `clamp(320px,52vh,440px)`（矮视口收缩；320px 下限为对指南 9.2 的 360px 下限的有记录偏离）。Pagination 多页换行未处理（现数据量不触发） | 用户要求「完善响应式设计」；修复均为窄屏真实溢出/挤压场景，不改变桌面端排版；断行策略按元素分级（容器 break-word、URL/代码 anywhere）避免过度断行 | `prose.css`、`ArticleLayout`、`archive/index.astro`、`SeriesCard`、`ProjectCard`、`HomeSection`、`PrevNextNav`、`index.astro` |
| IMPL-028 | 2026-09-17 | hero 品牌图修正（IMPL-025 验收反馈）：① 以像素亮度阈值扫描插画得图标包围盒（506,86–933,532，中心 719.5,309），按几何中心对称重裁 extract(470,59,500,500)——原裁切左边距 51px/右边距 17px 明显偏离居中；② 删除 `.hero-brand img` 的 CSS `border`——插画自带描边，叠加后呈双重边框，仅保留 20px 圆角柔化裁切外缘 | 用户反馈「图像不在截图块正中」「变成两重边框」；裁切参数由脚本求包围盒而非目测，避免再偏 | `src/pages/index.astro`、`public/home-brand.webp` |
| IMPL-029 | 2026-09-17 | Logo/favicon v2（IMPL-026 验收反馈「不够美观、不要蓝色底」）：底色改为插画原本的浅色渐变（#fafcfd→#e2eaef），图形反转——灯塔/主线海浪用品牌蓝 `#3f789f` 实体剪影，塔顶/光束/灯点/辅线海浪用贝壳粉系；`favicon.svg` 新增粉色辅波（呼应插画的双色波浪）；`Logo.astro` 同构并加 `#d3dee5` 细描边（浅色页头下界定浅色底轮廓；深色页头下浅色块自然醒目） | 用户方向性反馈；浅底+深色剪影直接复刻用户选定插画的观感，比蓝底反白更贴品牌原稿；16/32px 栅格化实测剪影清晰；粉 `#d9a0af`、蓝 `#3f789f` 均取自 tokens 品牌 | `public/favicon.svg`、`Logo.astro` |
| IMPL-030 | 2026-09-17 | `/posts/` 列表页系列折叠（用户需求：同系列文章在列表收束为一张可展开卡片）：`lib/content.ts` 新增 `FeedEntry = Post \| SeriesGroup` 与 `getPostFeed()`——同系列折叠为一个系列组，位置取系列最新一篇在时间线中的位置，**折叠先于 paginate**（同一系列不会跨页拆开）；新增 `SeriesFeedCard`（原生 `<details>`：摘要行 = 「系列」+ 名称 + N 篇徽标 + 旋转箭头，展开按阅读顺序列出篇目 = 01 序号 + 标题 + 日期，整行可点、悬停淡底出血，视觉语言与文内 `SeriesCard` 一致）；`Pagination` props 从 `Page<Post>` 放宽为 `Page<unknown>`（组件只读 url/页码，条目类型无关） | 用户指出此前折叠只落在文章详情页，列表页 `/posts/` 仍逐篇平铺；系列组计入一条分页额度，pageSize 语义改为「10 条」；窄屏（<640px）徽标/日期取消右推与 IMPL-027 同策略 | `src/lib/content.ts`、`src/components/content/SeriesFeedCard.astro`、`src/pages/posts/[...page].astro`、`Pagination.astro` |
| IMPL-031 | 2026-09-17 | `SeriesFeedCard` 外形改为列表行（IMPL-030 验收反馈「与其他文章外形类似，避免页面设计冲突」）：去掉卡片边框/左 3px 品牌边/`--surface` 底色/额外 margin，改为与 `ArticleListItem` 同构的扁平行——相同行距（1.25rem）、相同悬停语言（负外边距出血 + 品牌色 6% 淡底）、`<details>` 上下 `border-block` 承担与相邻条目的分隔（`.article-item + .article-item` 的 border-top 链在非同类兄弟处断开，由本元素补齐；`:first-child`/`:last-child`/相邻系列下去重避免列表首尾与双线冗余）。摘要行 = 「系列」小标 + 系列名（1.2rem/700，与文章标题同层级）+ 右侧旋转箭头 + 「共 N 篇 · 更新于 ×」meta 行；篇目列表左缘与列表列对齐（不再内缩） | 用户反馈卡片形态与列表页扁平风格冲突；保留 `<details>` 折叠交互与篇目行悬停语言不变，仅替换容器视觉；文内 `SeriesCard` 维持卡片形态（正文流中无冲突）不动 | `src/components/content/SeriesFeedCard.astro` |
| IMPL-032 | 2026-09-17 | 品牌标志收敛为单一图形源（用户决策，补记）：`favicon.svg` 定稿为「海湾灯塔」构图（浅雾蓝底 `#EDF4F7` + 细描边 `#DCE7EC`、贝壳粉光束、品牌蓝塔身、湾流曲线填充 + 描线双语言）；`Logo.astro` 放弃内联 SVG 第二份拷贝，改为 `<img src="/favicon.svg">` 直接引用——页头/页脚/加载页与浏览器标签页共用同一文件，图形不会再逐渐偏离 | 用户在验收中手改了 favicon 构图并同步调整 Logo 引用方式；此前 IMPL-026/029 两处独立维护矢量拷贝的做法被证实易发散，单一来源是结构性修正；trade-off：标志不再能随主题切换配色（当前为固定浅底构图，两种主题下均可辨识） | `public/favicon.svg`、`Logo.astro`、`src/assets/brand/README.md` |
| IMPL-033 | 2026-09-17 | 系列折叠条目信息密度重做（第三版，用户需求）：收起态对齐普通文章行的三层结构——标题行（「系列」小标 + 系列名 + 展开箭头）、元信息行（唯一分类入口 · 共 N 篇 · 合计约 M 分钟 · 更新于最新篇日期）、系列导语（阅读顺序首篇 description，两行截断）；展开态每篇透出序号、标题、日期、时长与两行截断摘要 | 用户指出收起态与展开态均需与普通文章同级的信息量，且与文内 SeriesCard 形态区分；合计时长/篇数由构建时 reduce 派生，首篇 description 按写作约定即系列开篇框架说明；与文内紧凑卡（仅定位 + 本篇标记）刻意保持两种形态：列表要有发现力，文内只需定位 | `src/components/content/SeriesFeedCard.astro` |
| IMPL-034 | 2026-09-17 | 系列条目补齐标签行 + `<details>` 展开动效（用户需求）：① 收起态在系列导语下新增标签行——全系列标签并集（阅读顺序去重，`TagList max=4`，与文章行 max=3 同量级），收起态自此与文章行的标题/元信息/摘要/标签四层同构，高度观感一致；② 展开动效零 JS 渐进增强：`html { interpolate-size: allow-keywords }` 全局开启 + `::details-content` 的 block-size 0↔auto 过渡与淡入（Chromium 129+，不支持时原生瞬时展开、无降级成本），列表页篇目行另加错峰上浮（`--i` 内联注入 + `animation-delay: min(calc(var(--i) * 45ms), 270ms)` 封顶）；文内 SeriesCard 仅高度 + 淡入、无错峰（正文流动效更轻）；reduced-motion 由既有全局规则自动关闭 | 用户指出系列条目与文章行「尺寸不同」源于缺标签药丸、并要求展开有美观动效；标签药丸即普通文章行的 TagList，frontmatter 并不缺字段；动效偏离指南 8.6「仅颜色变化」白名单系用户点名需求，幅度控制在 6px/320ms 内且可被 reduced-motion 关闭 | `src/components/content/SeriesFeedCard.astro`、`src/components/content/SeriesCard.astro`、`src/styles/global.css` |
| IMPL-035 | 2026-09-17 | 系列折叠扩展到分类页 + 条目高度节奏对齐（用户需求）：① `lib/content.ts` 抽出 `groupSeriesByName`/`collapseSeries` 共用逻辑，新增 `getCategoryFeed(key)`——分类内先过滤再折叠，跨分类系列在分类页只呈现该分类的篇目；`categories/[category].astro` 改为 feed 渲染（SeriesFeedCard 与 ArticleListItem 混排，与 /posts/ 同构），页面文案的「N 篇」按展开后真实文章数计；② `SeriesFeedCard` 收起态 summary 改为 flex column + `gap: 0.5rem` + `padding-block: 1.5rem`，去掉元信息/导语的零散 margin-top 与导语两行截断——与 `.article-item` 及 `.item-description` 逐属性同规格，标签行间距（用户点名的「tab 边距」）随之与文章行一致，两种条目占据的高度节奏统一 | 用户要求系列在「对应栏目」页面同样以 /posts/ 形式展现，且系列与文章行「方块卡片高度一致更协调」；系列详情页 `/series/[slug]/` 本身即系列专页（有序全列出），不套用折叠形态；导语截断移除后与文章行摘要行为完全一致（schema 限 200 字，极端长度同文章行自然换行） | `src/lib/content.ts`、`src/pages/categories/[category].astro`、`src/components/content/SeriesFeedCard.astro` |
| IMPL-036 | 2026-09-17 | hero 品牌图沿自带边框重裁 + 投影与动效（用户需求）：像素扫描确认插画为「圆角卡片（429×460，圆角约 96px）+ 下方 Cove｜小湾字标」构图，`home-brand.webp` 从 500×500 方形含底裁切改为沿卡片外缘精确裁切（429×460），四角残留底色由 CSS `border-radius: 22.5% / 21%` 对齐卡片圆角裁除——深色模式色差的根源（卡片外一圈浅色底）自此不存在，删除 `.dark` 下的 opacity 0.92 补丁；新增双层悬浮投影（浅色模式品牌蓝低透明度、深色模式深色投影，悬停加深）；动效三件套：入场淡入上浮（560ms）、缓慢上下漂浮（7s alternate ±4px）、悬停 `scale: 1.025`（独立属性，与 transform 漂浮动画叠加互不干扰）；位置微调：`align-self: center` + `margin-inline-end: clamp(0, 1.5vw, 1rem)` 补回裁掉的四周底色间距，尺寸 24vw/296px 收为 22vw/288px 适配竖版比例 | 用户指出深色模式下「边框外区域有很大色差」并提出沿边框裁切 + 加阴影动效；原裁切把卡片四周约 30px 浅色底一并带入，暗背景下呈亮色方块；包围盒下缘曾误含字标与贴边软投影（逐行墨水扫描区分开）；reduced-motion 下入场/漂浮/悬停由全局规则自动关闭 | `public/home-brand.webp`、`src/pages/index.astro` |
| IMPL-037 | 2026-09-17 | hero 品牌图抠底为透明 + 投影改按形状投射（IMPL-036 验收反馈）：IMPL-036 的沿边框裁切仍遗留两处瑕疵——① 卡片底缘是 squircle 曲线（网格拟合圆弧失败，均方误差 23），CSS 几何圆角无法贴合，四角外残留底色区块；② 卡片底描边（亮度 ~174–198）下方有一圈贴边软投影（~225–242），裁到 y=545 把投影中段切出一条硬边，即用户看到的「下方未裁剪区块」。改为**逐列描摹描边曲线烘焙 alpha**：每列自下而上找首个连续 ≤233 亮度行（描边核心，与投影分离干净）、自上而下找首个 ≤242 行，列间裁外留白 + 边界 2px 羽化，卡片外区域（含贴边投影）全部透明化——`home-brand.webp` 变为 429×463 带 alpha；CSS 弃用 border-radius + box-shadow，投影改 `drop-shadow` 双层（按 alpha 形状投射，浅色模式品牌蓝染色、深色模式纯黑加强 0.62/0.46，悬停同步加深）——深色模式此前虽有投影但叠在被裁掉的浅色矩形上，观感近乎不可见 | 用户反馈「图片下方还有未裁剪区块」并问深色模式是否也应有阴影；投影本就存在但 box-shadow 只能跟矩形且被 alpha 缺失抵消，抠底后 drop-shadow 真正按卡片形状落在两种主题上；squircle 无法用圆角近似是放弃 CSS 裁切改烘焙 alpha 的直接原因 | `public/home-brand.webp`、`src/pages/index.astro` |
| IMPL-038 | 2026-09-17 | 归档页空状态统一为 `EmptyState` 组件（Phase 2-5 交付产物审计发现）：空分支从裸 `<p class="archive-empty">` 替换为全站标准空状态（标题 + 说明 + 「全部文章」次级按钮），删除对应孤立样式 | 审计对照指南 19 Phase 2 检查表发现归档页是唯一未用 EmptyState 的列表页，空仓库场景下观感与其余页面不一致；修复后 `pnpm build` 0 错误，产物规模不变（45 页 / Pagefind 14 页 1149 词） | `src/pages/archive/index.astro` |
| IMPL-039 | 2026-09-17 | Pages CMS 接入：仓库根目录新增 `.pages.yml`（settings.content.merge + posts-media/projects-media 双媒体源 + 文章/笔记/项目三类 collection），交付 `docs/PAGES-CMS-GUIDE.md` 写作与发布说明，`src/assets/{posts,projects}` 补 `.gitkeep`（Git 不追踪空目录，否则推送后 CMS 媒体源指向不存在的路径）；字段与 `content.config.ts` schema 逐项对齐（category/lang/status/links.type 用 select 下拉锁定枚举，date 字段 `options.format: yyyy-MM-dd` 输出 schema 要求的日期字符串，filename 统一 `{primary}.md`）；与指南 12 示意配置的四处偏差：① category 由示意 string 改 select（指南 12 自身要求字段与 schema 同步）；② `filename` 采用官方仓库实测的字符串形式而非示意的 `{template, field: create}` 对象形式——后者无法在当前版本离线核实；③ `rename: safe`、image 字段级 `media:` 命名源两处键名保留示意写法但列入 GUIDE「上线前验证清单」；④ relatedPosts 用 string list + 手填 ID 而非 reference 字段（reference 写入路径而非裸 ID，与 schema 及 `lib/content.ts` 校验不符） | 指南 19 Phase 6 任务「配置 Pages CMS 的三类内容与媒体配置」；Pages CMS 文档站抓取通道受阻（web_reader/WebFetch 均失败），语法经搜索引擎摘要 + 官方 pagescms-website 仓库 `.pages.yml` 实例交叉核实，指南 12 明确要求示意配置须经真实测试文章验证，故不可离线验证的键全部集中到 GUIDE 第 7 节清单，首篇测试文章逐项确认；媒体 output 用相对路径 `../../assets/posts` 是 Astro `image()` 相对解析的要求，与常规 CMS 的 URL 前缀写法不同 | `.pages.yml`、`docs/PAGES-CMS-GUIDE.md`、`README.md`、`src/assets/posts/.gitkeep`、`src/assets/projects/.gitkeep` |
